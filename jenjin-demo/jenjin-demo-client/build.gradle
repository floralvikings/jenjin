apply from: 'javafx.plugin'

dependencies {
    compile project(':jenjin-world-client')
}

javafx {
    mainClass = 'com.jenjinstudios.demo.client.JenjinDemoClient'
}

jfxDeploy {
    enabled = false
}