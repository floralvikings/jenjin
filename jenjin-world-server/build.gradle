description = 'Basic Jenjin-Based MMO Server'

dependencies {
    testCompile 'com.h2database:h2:1.4.178'
    compile project(':jenjin-core-server')
    compile project(':jenjin-world-core')
}

test {
    maxParallelForks = 1
    options().parallel = 'methods'
}